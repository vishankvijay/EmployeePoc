<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="LoginActivityTheme" parent="AppTheme">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    <style name="TextInputStyle" parent="Widget.MaterialComponents.TextInputLayout.OutlinedBox">
        <item name="boxStrokeColor">@color/colorPrimary</item>
        <item name="errorEnabled">true</item>
    </style>

    <!-- CardView Style -->
    <style name="CardViewStyle">
        <item name="android:background">@android:color/white</item>
        <item name="cardCornerRadius">@dimen/card_view_radius</item>
        <item name="cardElevation">@dimen/card_view_elevation</item>
        <item name="contentPadding">@dimen/card_view_content_padding</item>
    </style>

    <!-- RecyclerView Style -->
    <style name="RecyclerViewStyle">
        <item name="android:layout_marginLeft">@dimen/recycler_view_margin</item>
        <item name="android:layout_marginRight">@dimen/recycler_view_margin</item>
        <item name="android:layout_marginTop">@dimen/recycler_view_margin</item>
        <item name="android:layout_marginBottom">@dimen/recycler_view_margin</item>
    </style>

    <!-- Material Button Style -->
    <style name="MaterialButtonStyle" parent="Widget.MaterialComponents.Button.OutlinedButton">
        <item name="android:padding">12dp</item>
        <item name="strokeColor">@color/colorAccent</item>
    </style>

</resources>
